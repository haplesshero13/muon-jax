[project]
name = "muon-jax"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "jax>=0.8.1",
    "optax>=0.2.6",
]

[dependency-groups]
dev = [
    "pytest>=9.0.1",
]
